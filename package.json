{
  "name": "react-beat-map",
  "version": "1.0.0",
  "description": "A ReactJS musical instrument",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/scripts.babel --out-file build/scripts.js && sass src/styles.scss:build/styles.css && cp src/index.html build/index.html"
  },
  "author": "Adam Giese",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  }
}
